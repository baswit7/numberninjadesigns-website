# Changelog

All meaningful production-facing and architectural changes are recorded here.
Entries follow semantic intent rather than documenting every commit.

## Unreleased

### Added

- Production foundation documentation for architecture, delivery workflow,
  roadmap, issues and improvement candidates.
- Modular ownership directories for `core`, `modules`, `api`, `automation`,
  `assets`, `docs` and `archive`.
- Repository hygiene configuration through `.editorconfig` and `.gitignore`.
- Consistent LF text normalization policy through `.gitattributes`.

### Preserved

- Existing public root page, privacy page, TikTok callback page and TikTok
  verification content and behaviour remain unchanged. Future text changes
  follow the repository line-ending policy.

## Existing Baseline - 2026-05-24

- Repository contains a static root verification host page.
- Repository contains a public privacy page and TikTok OAuth callback path.
- Repository contains TikTok site verification text files.
