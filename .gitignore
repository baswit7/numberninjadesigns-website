# Ephemeral credentials generated by google-github-actions/auth.
gha-creds-*.json

# Local environment files can contain provider credentials.
.env
.env.*
!.env.example

# Search Console reports must remain outside the public repository.
gsc-readonly-report*.json

# Google factory reports contain private Drive identifiers and validation data.
finance-google-factory-report*.json
google-factory-*.json

.vercel
.env*
