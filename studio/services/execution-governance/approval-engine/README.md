# Approval Engine

The approval engine validates whether a future execution request has a modeled human approval state.

It reads declarative approval records and writes only governance summaries. It does not send notifications, authenticate users, call identity providers or auto-approve execution.
