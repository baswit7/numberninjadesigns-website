# Dependency Engine

Models declared dependencies for future execution plans. It can state whether dependency evidence is present, missing or out of scope, but it cannot fetch, install, call or mutate dependencies.
