# Approval Chain Readiness

Approval-chain readiness describes the reviewers, review order and missing evidence required before a future execution request can be evaluated.

Approval-chain readiness is not approval. The model keeps `approvalGranted: false` and `executionAllowed: false` so reviewability cannot become permission.
